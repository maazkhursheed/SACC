// leave off @2x/@3x
const images = {
  // logo: require("../Images/logo/logo.png"), although if images gonna use will be in this format but better is to use .svg icons/images
};

export default images;
